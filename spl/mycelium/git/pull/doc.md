---
summary: Pull commits from the remote, subtree-aware.
---

Pulls the tracking remote. Two-reality aware: inside a
subtree, pulls via subtree semantics (merges upstream into
the prefix). From the outer repo, pulls the outer repo.
