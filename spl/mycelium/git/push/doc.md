---
summary: Push commits to the remote, subtree-aware.
---

Pushes the current branch to its tracking remote. Two-reality
aware: invoked inside a subtree, pushes to the subtree's
remote via subtree push semantics. From the outer repo,
pushes the outer repo.
