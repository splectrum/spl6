{
  "type": "record",
  "name": "subtree_push_input",
  "namespace": "spl.mycelium.git.subtree.push",
  "doc": "Input shape for one git.subtree.push invocation.",
  "fields": [
    {
      "name": "prefix",
      "type": ["null", "string"],
      "default": null,
      "doc": "Subtree prefix. Null means infer from subtree reality — required when invoked from the outer repo."
    }
  ]
}
