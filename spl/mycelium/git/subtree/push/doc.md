---
summary: Push subtree changes upstream.
---

Pushes the local subtree's history to its registered remote.
Inside the subtree prefix, the prefix is inferred from the
execution root; from the outer repo, pass the prefix as
input.
