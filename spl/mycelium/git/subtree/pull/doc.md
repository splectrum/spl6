---
summary: Pull subtree changes from upstream.
---

Merges upstream changes into the local subtree. Inside the
subtree prefix, the prefix is inferred; from the outer repo,
pass the prefix as input.
