---
summary: Show working-tree diff, two-reality aware.
---

Returns the full unified diff plus a summary (files changed,
insertions, deletions). Two-reality aware: from inside a
registered subtree, scopes to that subtree's paths.

Response schema: `spl.data.mycelium.git.diff`.
