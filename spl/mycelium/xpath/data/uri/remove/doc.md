---
summary: Remove the node at the fabric address, data lens, bytes variant.
---

Removes the node at `key`. Data lens — removing from a
metadata path returns an error. URI variant (schema-agnostic).
