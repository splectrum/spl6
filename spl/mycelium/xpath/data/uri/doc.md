---
summary: Data URI protocols — schema-agnostic, data-visibility bytes in/out.
---

URI variant of the data family. Combines the data lens
(metadata paths invisible) with URI simplicity (no type
inference, bytes pass through).

For schema-aware data access with type resolution and
into-file navigation, use siblings under `spl.mycelium.xpath.data`.
