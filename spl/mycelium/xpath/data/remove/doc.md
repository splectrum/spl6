---
summary: Remove the node at the fabric address, data lens, schema-aware.
---

Removes the node at `key`. Data lens — removing from a
metadata path returns an error.

For metadata removal use `spl.mycelium.xpath.metadata.remove`;
for schema-agnostic data removal use `spl.mycelium.xpath.data.uri.remove`.
