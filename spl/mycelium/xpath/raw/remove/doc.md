---
summary: Remove the node at the fabric address, schema-aware.
---

Removes the node at `key`. Raw visibility — removes
regardless of underscore-prefix segments.

For schema-agnostic removal, use `spl.mycelium.xpath.raw.uri.remove`.
