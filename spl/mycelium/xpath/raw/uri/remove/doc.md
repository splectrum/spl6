---
summary: Remove the node at the fabric address, bytes lens.
---

Removes the node at `key`. URI variant — schema-agnostic.
Raw visibility — removes regardless of underscore-prefix
segments.

For schema-aware removal, use `spl.mycelium.xpath.raw.remove`.
