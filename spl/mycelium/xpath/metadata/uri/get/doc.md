---
summary: Read bytes from a node at a metadata address.
---

Reads the node at `key` as bytes. Metadata lens — only
underscore-prefixed segments (or paths inside a metadata
subtree) are visible.

For schema-aware metadata reads use `spl.mycelium.xpath.metadata.get`.
