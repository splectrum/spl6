---
summary: Metadata URI protocols — schema-agnostic, metadata-visibility bytes in/out.
---

URI variant of the metadata family. Combines the metadata lens
(only underscore-prefixed paths visible) with URI simplicity
(no type inference, bytes pass through).

For schema-aware metadata access with type resolution, use
siblings under `spl.mycelium.xpath.metadata`.
