{
  "type": "record",
  "name": "get_input",
  "namespace": "spl.mycelium.xpath.metadata.get",
  "doc": "Input shape for one xpath.metadata.get invocation.",
  "fields": [
    {
      "name": "uri",
      "type": "string",
      "position": "key",
      "doc": "Node URI relative to local root. Non-metadata paths are invisible (metadata lens). Supports into-file navigation."
    }
  ]
}
